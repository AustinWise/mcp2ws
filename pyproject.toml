[project]
name = "mcp2ws"
version = "0.1.0"
description = "Expose SOAP web services as MCP servers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.25.0",
    "suds>=1.2.0",
]
